``` clojure
(println "Hello World")
(dotimes [_ 10]
  (println "hello"))
(defn main- [& args]
  (println "I got all this arguments" args " and now i'm returning nil."))
(defn square [x]
  (* x x))

```

print hello world print hello world 10

- dotimes
- \_ means we dont want to bind the value to a symbol.

create a procedure create a function create a 2D point data structure
